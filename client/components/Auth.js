const React = require('react');

const Auth = () => {
  return (
    <span className="header-item">
      <a className="button is-outline" href="#">Sign Up</a>
    </span>
  );
};

module.exports = Auth;
